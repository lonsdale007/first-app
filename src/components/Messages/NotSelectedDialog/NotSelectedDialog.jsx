import nsd from './NotSelectedDialog.module.css';

const NotSelectedDialog = (props) => {
    return (
        <div className={nsd.page}>
            Select a chat
        </div>
    )
}
export default NotSelectedDialog;